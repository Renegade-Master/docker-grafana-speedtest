apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: grafana-postgres-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/grafana-postgres-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/grafana-postgres-net: "true"
