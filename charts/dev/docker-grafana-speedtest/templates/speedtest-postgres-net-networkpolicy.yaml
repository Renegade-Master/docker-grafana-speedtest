apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: speedtest-postgres-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/speedtest-postgres-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/speedtest-postgres-net: "true"
